// Flutter imports:
import 'package:flutter/material.dart';

const pb1 = EdgeInsets.only(bottom: 16.0);
const pt1 = EdgeInsets.only(top: 16.0);
const pl1 = EdgeInsets.only(left: 16.0);
const pr1 = EdgeInsets.only(right: 16.0);

const ph1 = EdgeInsets.symmetric(horizontal: 16.0);
const pv1 = EdgeInsets.symmetric(vertical: 16.0);

const p1 = EdgeInsets.all(16.0);

const p2 = EdgeInsets.all(24.0);

const p3 = EdgeInsets.all(32.0);
